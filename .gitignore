# Credentials — never commit
.env

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# Output files (regenerated each run)
output/
logs/

# Recruiter lookup cache (pure quota-saving; rebuilt on demand)
data/recruiter_cache.json

# Dedup state — owned by GitHub Actions, not local runs
data/seen_jobs.json

# API runtime DB — rebuilt on first launch
data/api.sqlite

# macOS
.DS_Store

# Playwright MCP logs
.playwright-mcp/
